import { StyleSheet } from 'react-native';

export default homeStyle = StyleSheet.create({
    container: {
        flex: 2,
        justifyContent: 'center'
      },
      buttonContainer: {
        margin: 20
      },
      headerContainer: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
        margin: 20
      }
});